{
  "name": "final-final-web",
  "version": "0.2.44",
  "private": true,
  "scripts": {
    "build": "pnpm -r build && pnpm run docs",
    "dev": "pnpm -r dev",
    "docs": "node ../scripts/generate-getting-started.js",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "typescript": "^5.3.0"
  }
}

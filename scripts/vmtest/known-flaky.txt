# Test identifiers whose failure is expected and does not trip the circuit
# breaker (see scripts/vmtest/README.md and the plan's §3).
#
# One fully-qualified test identifier per line, exactly as it appears in
# xcresult's failing-test output (Target/Class/method). Blank lines and lines
# starting with # are ignored.
#
# Widening this file mid-crisis (any streak non-zero) is refused by `vmtest`
# and by the uitest-ready-gate hook — see the plan's §3 and §4.

# Re-added 2026-08-06 after test-isolation fixes (t-2d6b4898) ruled out
# fixture pollution as the cause: a 4th full-suite validation run reproduced
# a genuine failure with a real block-id dump attached (headings instead of
# an ordered_list, IDs never seen before -- not stale/leftover state). This
# is believed to be the app-level race filed as t-3904c457 (mode-toggle
# flush not paired with pushBlockIds), not a test-infrastructure bug. Keep
# this quarantined until that root cause is fixed, or it will keep re-arming
# the circuit breaker (see t-5f48547e).
#
# UPDATE 2026-08-28: t-3904c457's researched root cause (a straddling poll's
# stale insert appended at document end with a brand-new id) is now guarded
# against in Database+BlocksInsert.swift's processEditorInserts — a non-temp
# insert anchor that no longer resolves in the DB rejects the insert instead
# of falling through to append-at-end (unit-tested in
# BlockInsertAnchorTests.staleAnchorInsertIsRejected). The task note left one
# alternative open (typing-flakiness: the test file itself documents dropped
# keystrokes), so keep this entry until a few full-suite runs come back with
# no absorption recorded for it (`vmtest stats` shows the count), then remove.
final finalUITests/ListNumberingE2ETests/testDeliberateNewListRestartsAtOne

# Added 2026-08-07 after a full-suite wrap-up run (superdev batch:
# codemirror-teardown-observers, sidebar-heading-numbers,
# zotero-export-decode-error) failed on "Status bar should reappear after
# exiting focus mode". None of that batch's three merges touch Focus Mode or
# status-bar code, and the task board already carries open, unrelated Focus
# Mode bugs (focus-preferences persistence, editor-pane resize in focus
# mode). An immediate scoped re-run of just this test passed cleanly, so
# this reads as pre-existing flakiness in an already-troubled area, not a
# new regression. Quarantined rather than root-caused; revisit if it starts
# failing consistently.
final finalUITests/EditorSmokeTests/testFocusModeToggle

# Added 2026-08-22 during Phase D of the unified-undo remediation
# (docs/plans/steady-unwinding-ledger.md). This test performs 4 rapid
# native-NSMenu-driven structural ops (right-click card -> Duplicate Section,
# back to back with no user pause) then immediately synthesizes Cmd-Z to
# verify the lowered eviction cap. The undo keystroke is consistently not
# observed anywhere in this app's own code -- not in native
# UndoRedoCommands.performUndo() (confirmed via an unconditional diagnostic
# log added specifically to test this, which never fires), and not even at
# handleSlashKeydown's earliest possible JS-side keydown observation point
# (same result: silent).
#
# Two hypotheses were eliminated with direct evidence: the structural-op
# latch (grepped every assignment site in undo-coordinator.ts -- forward ops
# never touch it, so 4 duplicates cannot leave it held), and insufficient
# settle time (widened from 1.5s to 2.0s between the last menu interaction
# and the keystroke, no change).
#
# CORRECTION (post-review): an AppKit-vs-DOM focus split was NOT actually
# eliminated, despite an earlier version of this note claiming it was. The
# accessibility tree only proves DOM focus (document.activeElement inside
# the WebView) -- it says nothing about NSWindow.firstResponder, the real
# native value a Cmd-Z key equivalent depends on. The only instrumentation
# that reads that real value lives inside UndoRedoCommands.performUndo()
# itself, which -- as stated above -- never fires at all for this failure.
# That means the AppKit-focus hypothesis was never actually tested; it's
# moot only in the sense that performUndo() never runs regardless of what
# the real first responder is, not because the responder was confirmed
# correct. A judge review (independent evidence, not this note) separately
# confirmed the quarantine's OUTCOME still stands: a disabled NSMenu item
# doesn't consume its key equivalent at all, disabling requires no project
# window to be key (not the case here), and testDeleteAndDuplicateSectionUndo
# proves the identical duplicate-then-undo sequence works once -- ruling out
# "duplicating disables Undo" as the mechanism. The test itself now asserts
# app.menuItems["menu-undo"].isEnabled immediately before Undo #1's keystroke
# as the decisive check going forward: if it's ever found disabled there,
# reopen this quarantine immediately, this note's reasoning no longer holds.
#
# With that ruled out (by the menu-item check, not by the AX tree), what
# remains is XCUITest's own synthetic key delivery failing to reach the app
# immediately after a run of native NSMenu tracking-loop interactions -- a
# test-infrastructure limitation, not a defect in this app's undo/eviction
# logic (which the equivalent Swift unit-level eviction tests already cover
# without this specific UI-driving hazard). Revisit if eviction-cap
# regression coverage needs strengthening; this quarantine does not remove
# unit-level coverage of the eviction behavior itself.
final finalUITests/UnifiedUndoE2ETests/testEvictionCapEvictsOldestEntryAndStaysConsistent

# Added 2026-08-27 during sd-wrap-up for t-18576cf7 (blank-document-open fix). Second
# occurrence of the identical failure -- "First Cmd-Z should undo the automatic heading
# correction, NOT the user's own typing -- the marker text should still be present
# immediately afterward" -- first seen 2026-08-25 during an unrelated wrap-up
# (bib-heading-false-positive) and deferred as t-e006ebc5. Neither branch that has
# reproduced this touches heading-hierarchy auto-correction or undo/eviction code, and
# both times an immediate scoped re-run of just this test passed cleanly. Reads as the
# same suite-order/state-pollution class already documented for
# testDeliberateNewListRestartsAtOne and testFocusModeToggle above. Quarantined rather
# than root-caused; revisit t-e006ebc5 if it starts failing in isolation too.
final finalUITests/EditorModeSwitchUndoE2ETests/testUndoAfterHeadingBreakingEditInWysiwyg
